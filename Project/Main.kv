#adds all the .kv screens to one main .kv file so it can be loaded into the builder
#:include LoginScreen.kv
#:include DraftScreen.kv
#:include MainScreen.kv

#Adds each screen to a ScreenManager 
ScreenManager:
    LoginScreen:
    DraftScreen:
    MainMenu:

<Tabs>:
<OneLineAvatarIconListItem>:
    id: listitem
